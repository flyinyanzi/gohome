# 七夕小家 V1 原型

这是第一版可运行原型：
- 一个 HTML 应用承载玄关 + 家的总览 + 六个房间
- CSS / JS 分离
- 小纸条、愿望、梦境使用 IndexedDB 存在浏览器本机
- 书房保留技能五子棋 / 读心术入口
- 当前首页使用第一张统一手绘风视觉概念图
- 房间内先以交互骨架为主，下一步可逐间替换成独立手绘背景与透明物件素材

本地运行建议：
不要直接双击 index.html。用一个静态服务器启动，例如：
python3 -m http.server 8000
然后浏览器打开 http://localhost:8000/qixi_home_v1/

目录：
index.html
css/
js/
assets/images/
games/gomoku/
games/mind-reader/


## V1.1 客厅更新
- 客厅换成独立手绘背景
- 点击水杯：喝水反馈
- 点击落地灯：循环调节 4 档明暗
- 点击左下“慢一点”：显示/隐藏呼吸光圈
- 进入客厅 30 秒无操作：出现“这样待着也很好。”
- 唱片小游戏：把唱片拖到唱片机转盘中心；支持鼠标和触摸设备

这版先完成客厅的整体构图和主要动作，音乐、独立透明物件素材、多多真实形象会在后续继续精修。


## V1.2 修正
- 修正 `.hidden` 被 `.breathing-orb` 的 `display:grid` 覆盖的问题。
- 呼吸光圈现在初始隐藏，点击“慢一点”后才出现并开始呼吸动画。
- 如果客厅背景图未上传/路径错误，页面底部会明确提示缺少 `assets/images/living-room-v1.png`。


## V1.3 手机适配与唱片交互
- 首页和客厅改为固定 1536×1024 的画布舞台，手机横屏会完整缩放到屏幕内，不再裁切。
- 右下角新增 − / ⌂ / ＋ 缩放控制；放大后可拖动画面。
- 手机支持双指缩放，横竖屏切换后自动重新适配。
- 背景和交互热点一起缩放，避免手机比例变化造成错位。
- 唱片改成唱片套 + 唱片 + 转盘轴心；摆放成功后提示圆框消失，只留下旋转唱片。
- 多多动作、窗外互动、小纸条和音乐播放清单留到后续。


## V1.4 画面适配修正
- 修正 V1.3 的核心缩放坐标问题：不再使用 `left:50% / top:50% + translate(-50%) + scale()`。
- 改为左上角原点 + JS 精确计算缩放后宽高，再居中放置整张 1536×1024 画布。
- 手机横屏、竖屏、桌面端默认都以“整张图完整可见”为基准，允许上下/左右留黑边，而不再裁图。
- 切换到首页/客厅时会重新 fit，避免隐藏页面初次计算尺寸不准确。
- Safari/手机地址栏变化通过 `visualViewport.resize` 重新适配。
- 桌面端鼠标滚轮可直接缩放；按钮和手机双指缩放继续保留。
- 所有热点都仍在同一个 1536×1024 舞台里，与背景一起做同一矩阵变换，避免缩放造成动作区域错位。


## V1.5 手机完整显示修正
- 现有插画是 1536×1024，也就是 3:2，不是 16:9。
- 手机端不再尝试“铺满屏幕”；改成真正的 contain/letterbox：四边完整显示优先，允许出现深色留边。
- 自动适配优先使用 `window.visualViewport`，避免 Safari 地址栏/工具栏导致计算出的可见区域比实际屏幕大。
- 默认完整显示时额外预留约 6% 安全边缘，防止浏览器功能条遮住图片边缘。
- 缩小下限从 1 放宽到 0.62，所以即使特殊手机比例仍可以继续把整张图缩小。
- 首页和客厅的背景与热点仍使用同一张 1536×1024 坐标画布，缩放后不会相互错位。
- 后续如果确定整套以手机横屏为主，可以重新绘制一套 16:9 房间素材；但这版先不重画，以免反复调整所有热点。


## V1.8 修正：不再叠两套按钮，卧室真正可交互
- 基于 V1.5 继续开发。
- 首页 `home-concept.png` 本身已经画有六个房间按钮，所以这版不再叠加第二套可见 HTML 按钮；HTML 只做透明点击热点，位置按原图 1536×1024 坐标逐个校准。
- 因此首页视觉上只会看到背景图自带的一套按钮，不再出现“双按钮”和明显错位。
- 卧室继续使用当前框架图，但把图上已经画好的“梦境抽屉 / 小纸条 / 窗外”做成真实热点。
- 床头实体灯增加 4 档明暗调节。
- 点击床铺附近的月亮热点会打开“睡不着的时候”卡片，并可启动 8 秒循环的呼吸光圈。
- 点击“梦境抽屉”会打开原有 IndexedDB 梦境记录/改写界面。
- 点击“小纸条”会打开卧室自己的本地存档纸条。
- 点击“窗外”会出现一张很轻的夜景提示卡。
- 下一阶段再重新绘制真正“无 UI 烘焙”的首页/卧室纯场景背景，到时把全部按钮彻底交给 HTML/CSS。


## V1.9 新卧室图对齐
- `bedroom-v1.png` 已替换为新的 1536×1024 正式卧室原图，不再裁切或拉伸概念图。
- 床头灯、睡不着、小纸条、窗外、梦境抽屉热点按新图重新定位。
- 新增“摸摸多多”热点。
- 首页房间入口 HTML 热点彻底透明：包括 hover / focus / active 状态都不会出现半透明底色或光晕。


## V1.10 卧室功能补全
- 床头灯：点击实体灯区域，循环 4 档明暗。
- 睡不着：打开安静的小卡片，可选“跟着光慢一点”“把灯调暗”“就这样躺一会儿”。
- 呼吸光圈：8 秒缓慢循环，不要求数拍子。
- 小纸条：使用 IndexedDB 保存在当前浏览器，可再次打开查看。
- 窗外：显示轻提示，并让窗边星星短暂闪烁。
- 摸摸多多：出现文字反馈和几颗飘起的小爱心。
- 夜间模式：点击图片右下角“夜间模式”，整体降低亮度与饱和度，再次点击恢复。
- 梦境抽屉：继续保留梦境记录和改写结局功能。


## V1.11 功能修复
- 修复卧室其它功能完全不响应的问题：旧代码还在直接绑定已经不存在的 `#lamp`，导致 `interactions.js` 在加载中途抛错，后面的卧室事件全部没有注册。
- 现在旧节点监听都改成安全的可选绑定，不会再阻断脚本。
- 首页移除重复的“回来啦。今晚想在哪里待一会儿？”HTML 文案，只保留背景图里已经画好的文字。
- 客厅和卧室“回到家里”统一改为圆角半透明的“⌂ 回到小家”按钮样式。


## V1.12 卧室呼吸引导 + 返回按钮统一
- 修复“跟着光慢一点”只有文案、看不到画面变化的问题：原来的光圈被睡眠卡片挡住了。
- 点击后现在会收起睡眠卡，在床铺中央显示更明显的柔和光圈，8 秒一轮，交替提示“慢慢吸气 / 慢慢呼气”，共运行约 3 轮后自动结束。
- 客厅不再叠加额外的可见“回到小家”按钮；HTML 改为完全透明热点，覆盖在客厅背景图左上角已经画好的“回到小家”上。
- 卧室同样改为透明返回热点，覆盖新卧室图左下角已经画好的“回到小家”，两间房的返回方式与首页房间入口采用同一原则：图片负责视觉，HTML 只负责点击。


## V1.13 阳台正式场景
- 新增独立 1536×1024 阳台原图 `assets/images/balcony-v1.png`，没有从概念拼图裁切。
- 图片中的“回到小家 / 抓流星 / 写下愿望 / 愿望盒子 / 吹吹风 / 摸摸多多 / 夜间模式”均接入透明 HTML 热点。
- 抓流星：有短暂星光动画，并进入愿望书写。
- 写愿望：可选择保存在当前浏览器，或“送给星星”后不保存。
- 愿望盒子：查看本机已经保存的愿望。
- 吹吹风：开启/关闭轻柔风线动画。
- 摸摸多多：出现轻微爱心反馈。
- 夜间模式：进一步压暗阳台环境。


## V1.14 房间功能脚本隔离修复
- 卧室与阳台功能从旧的 `interactions.js` 后半段拆出，集中到独立的 `js/room-features.js`。
- `interactions.js` 只保留客厅和通用存档逻辑，避免某一个房间的旧代码报错后把后续所有功能一起阻断。
- 新功能脚本全部使用防御式节点绑定，即使某个可选元素不存在，也不会影响其他按钮。
- `index.html` 对脚本加入 `?v=1.14`，用于绕过 GitHub Pages / 手机浏览器对旧 JS 的缓存。
- 浏览器控制台会出现 `[gohome] room-features v1.14 loaded`，可用于确认新版脚本确实已经部署。


## V1.15 精确根因修复 + 结构加固

### 查明的根因
V1.13 之后已经没有 `[data-panel="wish-panel"]` 这个入口，但 `interactions.js` 仍然直接执行：

```js
document.querySelector('[data-panel="wish-panel"]').addEventListener(...)
```

`querySelector()` 返回 `null`，于是浏览器抛出 `TypeError`，`interactions.js` 从这一行起停止执行。
这正好解释了当时的现象：前面已经注册好的通用梦境抽屉还能打开，而后面才注册的卧室/阳台自定义功能全部失效。

### 本版修复
- 把 note / wish / dream 的旧入口监听统一改为 `bindArchiveTrigger()` 防御式绑定；元素不存在时直接跳过，不再抛错。
- `rooms.js` 也加入目标场景、目标 dialog 的存在检查。
- 保留 V1.14 的结构性改进：卧室和阳台继续放在独立 `room-features.js`，不再和客厅旧逻辑挤在同一条执行链上。
- 所有本地 JS 都增加 `?v=1.15`，降低 GitHub Pages / 手机浏览器使用旧缓存的概率。
- 控制台若看到 `[gohome] room-features v1.15 loaded`，说明新版房间功能脚本已成功加载。


## V1.16 窗边正式场景
- 新增独立 1536×1024 窗边原图 `assets/images/window-room-v1.png`。
- 使用新版缓存标记 `?v=1.16`。
- 窗边功能：
  - 晾衣服：点衣物，再点晾衣位置；床单步骤会出现一只与画面色调一致的辅助“另一只手”，只用动作表达协作，不写破含义。
  - 全部晾完后出现彩蛋文案：“今天的衣服也晾好了。”
  - 铲猫砂：点掉三个猫砂团，猫砂铲会有轻微动作；结束后多多已经在旁边等。
  - 吹吹风：开启/关闭柔和风线动画。
  - 摸摸多多：轻微爱心反馈。
  - 夜间模式：把傍晚色调压暗成安静的夜色。
- 窗边不再放小纸条，避免和卧室功能重复。

## V1.17 猫猫房第一层
- 新增独立干净背景 `assets/images/cat-room-v1.png`。
- 午后阳光光斑缓慢移动，尘埃持续漂浮。
- 多多可点击并有随机反馈。
- 照片墙接入 15 张真实照片并分类。
- 饭饭角、猫窝、椅子可浮出对应真实照片。
- 纸箱有约 25% 概率触发吐舌头照片彩蛋。
- 玩具区有第一版滚动小球；猫爬架有随机反馈。
- “今日多多”按本地系统日期做日期哈希，同一天固定一句。
- 所有 JS 缓存版本更新为 `?v=1.17`。
